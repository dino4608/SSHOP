export const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_URL;

// JWT
export const ACCESS_TOKEN_TTL = Number(process.env.ACCESS_TOKEN_TTL);
export const REFRESH_TOKEN_TTL = Number(process.env.REFRESH_TOKEN_TTL);

