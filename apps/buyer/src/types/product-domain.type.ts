export type Product = {
    id: number;
    name: string;
    price: number;
    category: string;
};

export type Category = {
    id: number;
    title: string;
    slug: string;
    image: string;
};